# screen_down_detector example

Run the example on a physical Android or iOS device:

```sh
flutter run
```

Place the device screen-down to blur the displayed balance. Tap the screen to
show it again.
